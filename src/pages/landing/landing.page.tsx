import React from "react";
import { observer } from "mobx-react-lite";
import "./landing.css";


const LandingPage = () => {

  return (
    <div>
      Landing page
    </div>
  );
};

export default observer(LandingPage);
