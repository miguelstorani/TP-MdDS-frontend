import React, { ReactElement } from "react";

const NotFoundPage = (): ReactElement => {
  return (
    <div>
      <p>
        404 Error: the requested url cannot be found
      </p>
    </div>
  );
}

export default NotFoundPage;